let map = loadMap('map', [39, -77], 5);

// Markers for Richmond and Annapolis
let richmond = addMarker(map, [37.5, -77.4]);
let annapolis = addMarker(map, [39, -76.5]);
// Add a marker for Washington D.C.
let washing = addMarker(map, [38.9, -77]);